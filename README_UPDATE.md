## Visual Prototype Note

The file `city.png` represents a generated prototype image used to explore the OpenSourceCity visual interface concept.

Original inspiration images are retained separately as provenance.

This image is used for:

- Clickable map testing
- Layout experimentation
- System visualization

It does not represent the final design.
